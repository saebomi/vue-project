.view
    .channel
        ul
            li(v-for="(channel) in channels") {{ channel }} 
            .channel-name {{ $route.params.cname}}
            .messages {{message}}
            .form
            input (type="text" v-model="message")